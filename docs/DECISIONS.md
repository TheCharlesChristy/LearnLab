# Decision Log

Ambiguities resolved by the orchestrator, per the smallest-decision rule (Appendix A alignment).

| ID    | SRS section | Ambiguity | Decision | Why |
|-------|-------------|-----------|----------|-----|
| D-001 | §8.7 P0 / §12 AC-01, AC-03/07 | P0 ships "no public content", but e2e gates need renderable content | `build-content.mjs` accepts `--root <dir>` (also needed by AC-01). E2E builds against `tests/fixtures/content/` fixture courses; `public/content/` stays empty until P1. | Keeps P0 spec-literal; AC-01 needs a temp-root mode anyway. |
| D-002 | §5.3 vs §8.7 | All v1 widgets are priority M, but the P0 phase row names only `function-grapher` | P0 ships `function-grapher`, `figure`, `quiz` (engine exists in P0). `data-plot`, `step-reveal` land in P1; `code-runner` in P1 (needs the worker); `logic-gate-sim`, `flashcards` in P2 per §8.7. | Phase table governs sequencing; M priority = required by release, not by P0. |
| D-003 | §3.2 | Dependency additions by parallel subagents would collide on `package.json` | T0.1 installs the entire fixed stack (§3.2) up front; thereafter only the orchestrator edits `package.json`. | Prevents parallel-task collisions on shared wiring. |
| D-004 | §4.5 / §5.3 | Who coerces directive attribute types — the Markdown renderer or each widget's `parseProps`? | Attributes pass to `parseProps` verbatim as strings (bare attribute → `true`). Widgets interpret their own prop types. | Pre-coercion corrupted numeric-looking string props (`expr="2"`, `alt="42"`); §4.5 assigns prop validation to widgets. Found by T0.9's doc cross-check. |
| D-005 | §4.1 vs FR-SHELL-001 | Course ids are unique only *within a subject* (§4.1), but the route `#/course/:courseId` keys by id alone | Course resolution scans subjects in catalogue order (maths→physics→cs→ai) and takes the first id match. | Smallest decision; §8 course ids are globally unique in practice. Revisit only if a collision ever ships. |
| D-006 | §6.2-6.3 | How does the host envelope cross the worker→Pyodide boundary into `_bridge.dispatch`? | worker.ts passes the envelope as a **JSON string**; `_bridge.dispatch` json.loads it (it already accepts str or dict). Worker→host stays a PyProxy, converted via `.toJs({dict_converter: Object.fromEntries})`. | A plain JS object reaches Python as a JsProxy (no implicit dict conversion), so `envelope.get(...)` would fail. Strings guarantee dict semantics and keep both unit suites (mocked, dict-based) green. The full real-Pyodide round trip is only exercised by the T1.12 `@py` smoke. |
| D-007 | §8.7 vs §8.2-8.5 | §8.7 P1 says "one [pilot] per subject" but `kinematics-suvat` is listed under the maths *alevel-mechanics* course (§8.2), not physics | Place each named pilot in the course §8.2-8.5 assigns it: differentiation-1→maths/alevel-pure, kinematics-suvat→maths/alevel-mechanics, boolean-algebra-and-logic→cs/alevel-cs, neural-networks-1-perceptrons→ai/ai-foundations. | The explicit named modules + their explicit course homes are authoritative; "one per subject" is loose descriptive intent. P1 pilots span maths(×2)/cs/ai; physics' first module lands in P3. The projectile item (§6.13b, AC-04/10) lives in kinematics-suvat. |
| D-008 | §6.2.2 | **Bug**, found via the real-Pyodide `@py` CI smoke: `worker.ts` calls `self.importScripts(pyodideBaseUrl + 'pyodide.js')` per §6.2.2 verbatim, but `host.ts` constructed the worker with `{type:'module'}` — module workers reject `importScripts()` outright, failing every Python item deterministically. | Construct the worker with `{type:'classic'}`. Vite's default `worker.format` is already `'iife'` (no override), which bundles `worker.ts`'s one runtime import (`./protocol`) into a self-contained classic script — no other change needed. | Diagnosed by installing the `pyodide` npm package locally (bypasses the sandbox's `cdn.jsdelivr.net` egress block, unrelated) to reproduce the worker↔`_bridge` boundary directly, then driving real headless Chromium against a local `vite preview` build to capture the exact browser error. |
| D-009 | §3.3, §6.2.2, §11 | **Bug:** `scripts/e2e-prepare.mjs` ran only `vite build`, never `build-python-bundle.mjs`. `public/python-bundle.zip` is gitignored, and the CI `e2e` job gets its own fresh checkout (no filesystem shared with the `web` job that runs the full `npm run build` pipeline) — so every Python item hung forever fetching a bundle that didn't exist. | `e2e-prepare.mjs` now runs `build-python-bundle.mjs` before `vite build`, mirroring the real `npm run build` pipeline order, and asserts `dist/python-bundle.zip` exists afterward. | Found via `logBrowserDiagnostics` (added to the `@py` specs) surfacing "loading LearnLab SDK…" with no further progress in the CI job log; confirmed by simulating a fresh checkout locally (removed the zip, showed it stayed missing from `dist/` under the old script). |
| D-010 | §6.7 | **Bug:** `RadioGroup`'s host component compared option *text* to `value` (`opt === value`, via a string-only prop reader that silently falls back to `''` for any non-string). `quiz.py` and the Python `RadioGroup` class both correctly pass/document `value` as the selected *index* (consistent with `on_change(index:int)` and with `CheckboxGroup`'s sibling index-based `values`) — so no radio could ever show checked, on any click, deterministically. | Read `value` via the numeric prop helper and compare by index: `checked={i === value}`, matching `CheckboxGroup`'s pattern and the Python class's own docstring ("Index of the selected option"). | Verified end-to-end against the real bridge (pyodide npm package): dispatching `EVENT(token, 2)` for the power-rule MCQ produces a `RENDER` whose `RadioGroup.value` is the number `2`. An initial (wrong) hypothesis blamed an async controlled-input race (§3.4); the 24-consecutive-failure pattern over a 10s poll ruled that out — timing races self-heal, permanent bugs don't. |
| D-011 | §5.8, FR-PWA-002/003 | **Bug:** `python-bundle.zip` matched neither the PWA precache glob (`**/*.{js,css,html,svg,png,ico,woff2}` — no `.zip`) nor either `runtimeCaching` rule (it's same-origin but root-level, not under `/content/`, and not the Pyodide CDN prefix). So it was completely unreachable offline, violating FR-PWA-003 ("any module fully viewed online... SHALL work offline thereafter") — caught by the AC-04 `@py` e2e test. | Added `zip` to the precache `globPatterns`. It's a normal `dist/` build artifact vite-plugin-pwa already content-hashes/revisions like any other precached file. | Confirmed the precache manifest grew from 63→64 entries and referenced `python-bundle.zip` in `dist/sw.js` after the fix. |
| D-012 | §3.5, §5.5, §5.3 flashcards | How should `flashcards` persist "grades stored in itemState" without importing `src/progress` directly, when the existing `recordAttempt` precedent is "context exposes raw `moduleId`, consumer assembles the record" — not a fully-curried per-item function — and `PyItemHost` (the other precedent) is a dedicated host component, not a context extension? | Extend `LessonContextValue` with `getItemState(itemId)`/`setItemState(itemId, state)`, pre-binding `moduleId` (a *more* curried shape than `recordAttempt`, deliberately). `itemId = `flashcards:${src}``. Wired in both `LessonPage.tsx` and `AssessmentPage.tsx` (two separate construction sites, confirmed by grep). | A native widget has no pre-mount lifecycle sequencing to do (unlike `PyItem`'s seed-derivation + saved-state-before-first-render), so a plain context read/write is proportionate — a considered choice, not a forced precedent match. Design reviewed by a Plan subagent before implementation; the `itemId` rule was a gap the review caught (no natural analog to `PyItemHost`'s `pyItemId()` existed before this). |
| D-013 | §5.3 logic-gate-sim | §5.3 only says "circuit JSON: nodes, wires" for `logic-gate-sim` — no literal schema given. | `{inputs: string[], gates: [{id, type: AND\|OR\|NOT\|XOR\|NAND\|NOR, inputs: string[]}], outputs: string[]}`. `gates[].inputs` reference an input pin name or an earlier gate's `id` (array order = eval order, acyclic by construction). Truth table: all `2^inputs.length` rows (capped at 6 inputs), row/column order = `inputs` array order. Not pipeline-validated (same lenient runtime-only pattern as `data-plot`/`step-reveal`). | The SRS wording is illustrative, not a literal required shape — documented explicitly so a future reader doesn't read it as a missed requirement. |
| D-014 | §7.3, FR-WID-002 | `docs/WIDGETS.md`'s own header already claimed "CI enforces this... the build fails" for widgets missing a doc section, but `build-content.mjs` never implemented the check — a real gap, not a missing nice-to-have, and P2 can't compliantly add two new widgets without it. | Implemented the check in `build-content.mjs` (always-on, not `--strict`-gated — FR-WID-002 is an invariant of "registering a widget", not a content-strictness rule) as a P2 precondition, in Wave 1 rather than deferred. Immediately backfilled the 3 P1 widgets it exposed as undocumented (`code-runner`, `step-reveal`, `data-plot`) so the newly-enabled check doesn't fail on existing content. | Found during P2 design-review exploration; fixing it before Wave 2 means the two new widgets are gated by the check from the moment they're written, not merged non-compliant and patched after. |
| D-015 | §5.3, §8.7 | Should any P2 content module actually use `flashcards`, or is it purely an engineering deliverable (like `logic-gate-sim`, which has no natural home in P2's maths/stats/mechanics scope)? | At least one `alevel-pure`/`alevel-statistics` module will embed a `flashcards` deck (e.g. key terms) to prove the widget end-to-end in real content. | Mirrors P1's practice of proving each new widget via real pilot-module usage, not just unit tests. Fulfilled: `proof` and `vectors` (both `alevel-pure`) each embed a `flashcards` deck. `logic-gate-sim` has no natural home in P2's maths/stats/mechanics scope and ships unused in content, same status as any engine capability awaiting its first consumer — unit-tested, not content-proven, until a CS/logic module exists. |
| D-016 | §7.3 test isolation | **Bug, found running Gate P2's full `vitest run`:** the FR-WID-002 doc-coverage regression test mutated the real repo `docs/WIDGETS.md` in place (backup/restore via try/finally) to exercise the "missing heading" failure path. `checkWidgetDocs` always reads that file via `repoRoot`, regardless of `--root` — so any other test file shelling out to `build-content.mjs` while vitest ran files in parallel could observe the mutated file mid-test, intermittently failing with a bogus "figure" doc-coverage error (observed in `scaffolders.test.ts`). | Added a `--docs-file <path>` CLI override to `build-content.mjs` (test-only seam, not part of the documented CLI surface, defaults to the real `docs/WIDGETS.md`). The test now points it at a scratch temp-dir copy instead of mutating shared repo state. | Mutating a real repo file from a test — even with try/finally restore — is unsafe under any concurrent test runner; the fix removes the shared mutable state entirely rather than trying to serialize around it. |
| D-017 | §12 AC-03, FR-SHELL-004 | **CI-observed, not locally reproducible:** Gate P2's real-CI run failed `progress-roundtrip.pw.ts` on `webkit` only (chromium + firefox green, and this test/code path predates P2 — untouched by the P2 diff), deterministically across all 3 Playwright retries (stable at 2 of 3 lessons showing complete, never reaching 3). `LessonPage.tsx`'s manual-completion handler set `completedNow` (driving the "Lesson completed" button/badge) *before* `await markLessonComplete(...)` resolved — an optimistic UI update that lets the button claim completion before the write is durable. This sandbox has no webkit binary (network-blocked) and the GitHub API token available here lacks `actions:write`/artifact-download scope, so the exact webkit mechanism (IndexedDB transaction timing vs. this UI race, most likely) could not be directly confirmed. | Reordered `complete()` to `await markLessonComplete(...)` before `setCompletedNow(true)` — the completed-button now only appears once the write is actually persisted, closing the specific race window regardless of the exact webkit timing quirk behind it. Pushed and re-verified via a fresh CI run rather than asserted from local-only evidence. | Never claim persisted state in the UI before the write resolves — cheap to fix, and correct independent of whether it was the confirmed root cause. If this run still fails on webkit, that's a real signal to investigate further (see BUILD_PLAN Gate P2 notes for the outcome). |
| D-018 | §4.7 pipeline behaviour, orchestrator process | **Process gap found mid-P3:** `build-content.mjs` silently skips full MVC validation for any module folder not yet referenced by its course's `course.json` (it only emits a `WARN ... not referenced by course.json`, not the shape/MVC checks). This is correct-by-design — an orphan folder isn't "shipped content" yet, and CI shouldn't fail on WIP scaffolding sitting in the tree — but it meant an early orchestrator read of "16 P3 modules already pass `--strict`" was wrong: 5 of those CS modules weren't in `course.json` yet, so several turned out to be bare, un-authored `new-module.mjs` scaffolds (TODO objectives, 1 lesson, 2-question assessment) that had never actually been checked. | Add every landed module's `ModuleRef` to the real `course.json` **as each module lands**, not batched at the end of the wave — this makes `--strict`'s output authoritative throughout the wave (real signal, not a false "all clear") instead of only becoming trustworthy once the final splice happens. | No code change: the skip behaviour is intentional and correct. The fix is procedural — orchestrator discipline about *when* to trust a green `--strict` run during multi-agent content waves, not a pipeline defect. |
| D-019 | Orchestrator process, session continuity | **Incident, not a spec ambiguity:** a session resumed from a stale checkpoint and redispatched all of Phase P3 from scratch, unaware that a separate earlier continuation of this same task had already completed P3 (Gate P3 CI-confirmed green) and landed 8 of 9 Phase P4 modules on the shared remote branch. Discovered only when the push was rejected (`fetch first`). | Diffed the redundant local P3 work against origin's — nearly identical, confirming origin's version (already real-CI-verified) was safe to prefer. Explicitly asked the user before doing anything destructive (a `git reset --hard` was correctly blocked by the harness's safety classifier); on approval, merged (not reset) and resolved the 4 small conflicts in favour of origin, preserving reflog/history instead of discarding local commits outright. | Never force-reconcile a diverged shared branch unilaterally, even when confident the remote is authoritative — a merge with conflict resolution is strictly safer than a hard reset (recoverable either way, and doesn't require guessing what the other side's uncommitted intent was). Re-verified with a fresh `build-content.mjs --strict` and `git diff --stat` audit after merging, rather than trusting the other session's self-report. |
| D-020 | §4.6 assessment authoring, `neural-networks-2-training` | **Bug, found during the orchestrator's mandatory 100%-verification pass:** `neural-networks-2-training`'s assessment q5 asked for the epoch count using the literal definition "reach a full epoch with zero weight updates," but by that exact definition the correct answer is 3 (epoch 2 itself still contains two weight updates; only epoch 3, re-run on the now-stable weights, has zero updates) — yet the stored answer was 2, matching the lesson's own convention ("converged after 2 epochs," treating the zero-update epoch as confirmatory, not as the counted epoch). | Reworded q5 to "first reach weights that correctly classify every example in the set," matching the lesson text and the `SimulationItem` trainer's own convergence check exactly, and expanded the explanation to name the epoch-3 zero-update pass as confirmation rather than the trigger. | A hand-verification pass caught what schema validation cannot: internally consistent JSON with a self-contradictory question definition. Worth the 100%-verification tier for computation-heavy modules — this is exactly the class of error P2/P3 already established that shape-checking alone misses. |
| D-021 | §13 roadmap, §5.5 schema | The spaced-repetition review queue needs per-item scheduling state (easiness factor, interval, due date) that doesn't fit any existing §5.5 table, and a grading scale — full SM-2 uses a 0-5 quality scale, but `flashcards` already ships a 2-button ("Again"/"Good") grading UX (D-012/D-015). | Added a new additive `reviewState` Dexie table (schema `.version(2)`, keyed `[moduleId+itemId]`) and a "SM-2-lite" 2-grade scale (`again`→quality 2, `good`→quality 4) implemented as a pure function in the new `src/progress/srs.ts`, consumed by `recordReview`/`seedReviewItem` in `db.ts`. `ProgressExport.exportVersion` bumped to 2 (`tables.reviewState` required for new exports); `importProgress` accepts v1 files by treating a missing `reviewState` key as empty (NFR-MAINT-002 backward compat), and merges v2 `reviewState` rows by the same newer-`updatedAt`-wins policy as `itemState`/`lessonProgress`. `LessonContext` (§3.5 boundary) extended with `recordReview`/`seedReviewItem`, pre-bound to `moduleId`, wired at both construction sites (`LessonPage.tsx`, `AssessmentPage.tsx`) exactly per the D-012 precedent — so `flashcards` and `QuizEngine` never import `src/progress` directly. `Flashcards.tsx` now also calls `recordReview` per-card (itemId `${deckItemId}:${cardIndex}`) alongside its existing per-deck `setItemState`; `QuizEngine.tsx` calls `seedReviewItem` for every missed question on finish (itemId `${quizId}:${questionId}`), skipped in practice mode like `recordAttempt`. | Reusing flashcards' existing 2-button UX (rather than introducing a 4/6-button full-SM-2 picker) keeps the grading interaction consistent across the app and matches "SM-2-**lite**" in the roadmap's own wording. `seedReviewItem` is a distinct, idempotent-if-already-tracked operation from `recordReview` (a real grade) so a quiz retry never clobbers an in-progress review schedule. |
| D-022 | §13 roadmap, §4.7 pipeline | Content search needs an index of lesson text, but the project's pinned dependency list (§3.2) has no search library, and adding one (Lunr/FlexSearch/etc.) would be a new dependency for a feature the SRS scopes as "still no server" / dependency-conscious. | `build-content.mjs` emits `content/search-index.json` (compact, gitignored like `index.json`) by re-walking the already-validated tree and stripping directive/markdown syntax from each lesson body to plain text. `src/search/` does dependency-free client-side token-count scoring (title ×3, module/course title ×2, body ×1) at runtime — no new npm dependency. | A second lightweight directory re-walk (rather than threading search-index data through `validateModule`'s internal bookkeeping) keeps the emission fully additive and independent of the validator's existing state — easier to review, harder to accidentally break existing validation. |
| D-023 | §4.6 v1→v2, quiz marking | The roadmap's "per-question partial credit for `multi`" supersedes §4.6 v1's explicit "no partial credit" rule for that one question type, but the SRS doesn't specify the exact scoring formula. | `scoreMulti(question, selected) = max(0, (correctPicks − incorrectPicks) / totalAnswers)`, in `[0, 1]`. `markMulti` (exact-match boolean) is kept alongside it for feedback wording — `QuizEngine`'s per-question `ResponseRecord` now carries both `correct` (exact match) and `points` (the score actually summed into the quiz total), with a third feedback/summary state ("Partially correct (NN%)") between "Correct!" and "Incorrect." `mcq`/`numeric`/`text` are unaffected (still exactly 0 or 1 point). | This is the standard "reward correct picks, penalise wrong picks, floor at zero" scheme (the same shape as Moodle's multi-response marking) — simple, explainable to a learner, and symmetric (selecting nothing scores 0, same as selecting only the wrong choices). |
| D-024 | §5.3, §13 roadmap (P3 widget candidates) | The SRS names `vector-field`, `geometry-canvas`, `circuit-sim`, `truth-table` as future widgets but (like `logic-gate-sim` in D-013) gives no literal prop/data schemas. | Pinned four schemas before dispatch (full detail in `docs/WIDGETS.md`): `vector-field` takes two mathjs expressions `fx`/`fy` plotted as an arrow grid (reuses `function-grapher`'s mathjs `compile`-only pattern, NFR-SEC-002); `geometry-canvas` takes a `{bounds, points, lines, circles}` scene JSON with keyboard-operable draggable points; `circuit-sim` takes a `{voltage, circuit}` JSON restricted to a recursive series/parallel resistor **tree** (deliberately not a general SPICE-like solver, so the maths stays exact); `truth-table` takes a boolean **expression string** (own hand-written recursive-descent parser, reusing `logic-gate-sim`'s AND/OR/NOT/XOR/NAND/NOR vocabulary and truth-table row/column convention) rather than a wired circuit — complementary to, not a duplicate of, `logic-gate-sim`. | Each schema deliberately reuses an existing pattern/convention from `function-grapher` or `logic-gate-sim` rather than inventing a new one, so the four widgets feel like one consistent family rather than four one-off designs. `circuit-sim`'s series/parallel-only restriction (no arbitrary graph topology) is a deliberate scope cut: it keeps the recursive resistance/current/voltage algorithm exact and unambiguous rather than reaching for a general linear-circuit solver this project doesn't need. |
| D-025 | §13 roadmap ("optional sync... still no server") | "Sync via user-owned storage" needs a concrete mechanism, but the SRS's own wording ("e.g. file-based export to cloud drive") is explicitly illustrative, not a literal spec, and the constraint "still no server" rules out anything beyond client-side/browser APIs. | File System Access API (`showSaveFilePicker`/`showOpenFilePicker`, Chromium-only) reusing the existing `ProgressExport` JSON shape verbatim via the already-shipped `exportProgress`/`importProgress` — a live file handle for manual "Save to synced file"/"Load from synced file" in Settings, not a new data format or a background daemon. Feature-detected; Firefox/Safari see an inline explanatory note and fall back to the already-shipped manual download/upload flow (never hidden entirely). The file handle is held only for the current page session (not persisted across reloads via IndexedDB handle storage) — a deliberate v1 scope cut. | A thin transport swap over already-verified export/import logic is far lower-risk than inventing new sync semantics or conflict resolution; "still no server" is satisfied by construction since the sync target is a file the OS/a cloud-drive client syncs, not anything this app talks to directly. |
| D-026 | §13 roadmap ("printable lesson view") | No schema/data decision needed — a CSS + one-button feature — but scope needed pinning: how much print polish is "enough." | A `@media print` stylesheet (forced light-on-white regardless of theme, nav/breadcrumb/progress-bar/action-row hidden via `print:hidden`, code blocks un-clipped and wrapping instead of horizontally scrolling, Python-item widgets height-capped so a tall interactive canvas can't print as multiple blank pages) plus a "Print this lesson" button calling `window.print()`. | Scoped as "good enough for a learner to print their notes," not pixel-perfect print production — matches the roadmap item's own modest framing, and avoids over-investing in browser-specific print-layout edge cases. |
| D-027 | Delivery-layer engagement work (not an SRS section — post-v1 UX initiative) | Streaks/points/achievements need persisted state, but `src/progress/types.ts` is headed "pinned... do not add or rename fields" for the SRS §5.5 tables, and `src/quiz`/`src/widgets` are documented as never importing `src/progress` directly. | Added a new, additive `engagement` Dexie table (`.version(3)`, singleton row keyed `'me'`) in a new non-pinned file (`engagement-types.ts`/`engagement.ts`), following D-021's exact precedent for growing the schema without touching the pinned interfaces. `ProgressExport.exportVersion` bumped to 3 (`tables.engagement`); older exports missing the key import as empty, same NFR-MAINT-002 tolerance as D-021. `LessonContextValue` gained one new fire-and-forget method, `notifyEngagement`, wired at both construction sites (`LessonPage.tsx`, `AssessmentPage.tsx`) exactly per the D-012/D-021 precedent, so `QuizEngine`/`Flashcards`/game widgets still never import `src/progress`. | The pin on `types.ts` protects the SRS-normative row shapes from drifting; it was never a ban on new, independent tables — D-021 already established the pattern this reuses. |
| D-028 | Delivery-layer engagement work — `matching-pairs` widget interaction model | A "learn through play" matching game could be built as literal HTML5 drag-and-drop (closer to the everyday mental model) or as click/tap-to-select pairing. | Click-to-select: click a left term, then a right term, to attempt a match; no native drag-and-drop. | The codebase's existing widgets hold a strong, consistent accessibility bar (full keyboard operability, no raw-pointer-only interactions). HTML5 drag-and-drop is not natively keyboard- or screen-reader-operable without substantial extra ARIA/live-region engineering; click-to-select gets full keyboard/AT support for free from ordinary `<button>` semantics, at no cost to the "feels like a game" goal. |
